apiVersion: apps/v1
kind: Deployment
metadata:
  name: workcluster
  labels:
    tier: workcluster
spec:
  replicas: 20
  selector:
    matchLabels:
      tier: workcluster
  template:
    metadata:
      labels:
        tier: workcluster
    spec:
      containers:
      - name: worker
        image: mr148/bioc-redis:RELEASE_3_14
        imagePullPolicy: Always
        resources:
          requests:
            memory: "800Mi"
            cpu: 500m
          limits:
            memory: 1Gi
            cpu: 1
        volumeMounts:
         - mountPath: /host
           name: local-provision
        command: ["R"]
        args: ["-f", "/home/docker/worker.R"]
      volumes:
        - name: local-provision
          persistentVolumeClaim:
            claimName: local-pvc-name
